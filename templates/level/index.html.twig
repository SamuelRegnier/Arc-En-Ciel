{% extends 'base.html.twig' %}

{% block title %}Niveaux Sarments{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="d-flex justify-content-center">Niveaux école Sarments</h1>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th colspan="2">Niveaux</th>
                </tr>
            </thead>
             <tbody>
                {% for level in levels %}
                    <tr> 
                        <td>{{level.name}}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    <div class="navigation d-flex justify-content-center">
        {{ knp_pagination_render(levels) }}
    </div>
</div>

{% endblock %}
