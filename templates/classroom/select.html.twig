{% extends 'base.html.twig' %}

{% block title %}Aperçu utilisateur{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="d-flex justify-content-center">Fiche Classe {{classroom.name }} {{classroom.year}}</h1>
            <div>
                <img style="max-width: 50%;" src="{{ vich_uploader_asset(classroom, 'imageFile') }}">
            </div>
            <div>
                <p>{{classroom.name}}</p>
                <p>{{classroom.year}}</p>
            </div>
            <div>
                <a href="{{ path('classroom_update', { id: classroom.id }) }}"><button>Modifier classe</button></a>
                <a href="{{ path('classroom_delete', { id: classroom.id }) }}"><button>Supprimer classe</button></a>
            </div>
    </div>
{% endblock %}